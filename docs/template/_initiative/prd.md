# PRD — \<Initiative name\>

> **This file uses the canonical PRD template.** To avoid two copies drifting apart, the full
> structure lives in **[`docs/template/prd.md`](../prd.md)**.
>
> **To fill it in:** copy the contents of `docs/template/prd.md` into this file, then complete it.
> Two things to remember:
> - The gate at the top — **link the validated Discovery** before writing the spec.
> - The **Feature breakdown** section is the bridge: it turns the need into named, prioritized
>   features. Each one becomes a folder under [`features/`](features/).

<!-- Paste the canonical PRD template below this line and fill it in. -->
